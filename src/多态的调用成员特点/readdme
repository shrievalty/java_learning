调用变量时：编译看左边，运行时也看左边
调用方法时：编译看左边，运行时看右边
多态的优势：在多态形式下，右边对象可以是西安解耦合，便于扩展和维护；
          定义方法的时候，使用父类类型作为参数，可以接收所有子类对象，体现了多态的可扩展性与遍历
多态的弊端：父类有的才能调用，如果没有直接报错，解决方案，将调用者变回子类类型，强制转换
         if a instanceof Dog
包：文件夹，管理不同功能的java类，方便后期维护
公司的域名反写+包的作用，需要全部英文小写，见名知意。com.itheima.domain 全类名，全限定名
如果使用同一个包的其他类不同导包，是同java.lang时也不用，其他情况都需要导包，如果同时使用两个包中的类时一定要用全类名，
final关键字，修饰后不能被改变，可以修饰方法，类，变量
修饰方法：最终方法，不能被重写，当一种方法是一种规则不想被改变，native调用本地其他语言
类：不能被继承，
变量：只能赋值一次
常量起名采用下划线，一个单词全部大写
final修饰的基本数据类型那么记录的值不能发生改变
final修饰引用型数据类型那么记录的地址值不能发生改变，内部的属性值还是可以改变的
权限修饰符：用来控制一个成员能够被访问的范围，一共四种，private<空着不写 <protected<public
实际开发当中一般只用pribvte和public，成员变量私有，方法公开；如果方法冲去其他方法中共性嗲吗，这个方法一般也私有
代码块：方法中的局部分代码块，构造代码块，静态代码块
静态代码块在前面加了static，随着类的加载而加载，并且自动触发、只执行一次
在类加载的时候，做一些数据初始化的时候去使用